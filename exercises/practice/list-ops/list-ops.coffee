class ListOps
  @append: (list1, list2) ->

  @concat: (lists) ->

  @filter: (list, f) ->

  @mylength: (list) ->

  @map: (list, fn) ->

  @foldl: (list, fn, initial) ->

  @foldr: (list, fn, initial) ->

  @reverse: (list) ->

module.exports = ListOps
